(jbuild_version 1)

(library
 ((name OBus_idl)
  (public_name obus.idl)
  (synopsis "DSL for defining D-Bus interfaces")
  (libraries (obus_data camlp4.lib))))

(rule
 ((targets (oBus_idl.ml))
  (deps    (oBus_idl.camlp4.ml))
  (action
   (run camlp4of -impl ${<} -o ${@}))))
