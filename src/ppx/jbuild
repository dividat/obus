(jbuild_version 1)

(library
 ((name ppx_obus)
  (kind ppx_rewriter)
  (synopsis "Utility syntax for defining D-Bus errors")
  (libraries (ocaml-migrate-parsetree ppx_tools))
  (preprocess (pps (ppx_metaquot)))))
